{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "siteName": {
        "value": "[concat('azuretodofunction', uniqueString(resourceGroup().id))]"
      },
      "storageAccountName": {
        "value": "[concat('azuretodofunctionstorage', uniqueString(resourceGroup().id))]"
      }
    }
}
